<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.teleinfo.bidadmin.blockchain.mapper.TrustanchorStakeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="trustanchorStakeResultMap" type="cn.teleinfo.bidadmin.blockchain.entity.TrustanchorStake">
        <id column="id" property="id"/>
        <result column="Owner" property="Owner"/>
        <result column="StakeCount" property="StakeCount"/>
        <result column="StakeTime" property="StakeTime"/>
    </resultMap>


    <select id="selectTrustanchorStakePage" resultMap="trustanchorStakeResultMap">
        select * from bif_trustanchor_stake where is_deleted = 0
    </select>

</mapper>
